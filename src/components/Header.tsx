import { Box, Typography } from "@mui/material"
import React from "react"

const Header = () => {
  return (
    <Box sx={{ height: 100, width: "100%" }}>
      <Typography>헤더임여</Typography>
    </Box>
  )
}

export default Header
